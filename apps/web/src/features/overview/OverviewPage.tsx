import { NotBuiltYet } from '../../components/NotBuiltYet'

export function OverviewPage() {
  return (
    <NotBuiltYet
      workPackage="WP5"
      summary="The exception-first cockpit: highest-severity alert with a corrective action, five KPI cards, the location risk table with current-versus-stale run labels, data freshness, latest planning activity, and secondary observed purchase-order activity."
    />
  )
}
