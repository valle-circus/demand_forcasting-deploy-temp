import { NotBuiltYet } from '../../components/NotBuiltYet'

export function DataSettingsPage() {
  return (
    <NotBuiltYet
      workPackage="WP3"
      summary="Planning inputs and maintained data: four upload cards for the master workbook, planning workbook, stock export, and cumulative purchase-order PDFs, each with source time separate from import time, validation issues with remedies, and version history, plus explicit master-version activation."
    />
  )
}
